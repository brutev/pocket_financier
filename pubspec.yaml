name: pocket_financier
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  fl_chart: ^0.68.0
  flutter:
    sdk: flutter
  flutter_gemma: ^0.11.13
  path: ^1.9.1
  path_provider: ^2.1.5
  sqflite: ^2.4.2
  flutter_sms_inbox: ^1.0.2
  permission_handler: ^11.3.1
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
